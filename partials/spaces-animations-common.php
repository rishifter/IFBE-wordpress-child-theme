// Snapping configuration
let snapConfig = {
  snapTo: 1,
  duration: {
    min: 0.2,
    max: 1,
  },
  delay: 0.01,
  // inertia: false,
  ease: 'power1.out',
};

// Scroll animation duration for hero section
let duration = 110;

// Stagger text duration for the slides
let staggerDuration = 0.1;
